---
title: RecordingMode Guide
description: Learn what RecordingMode does, how to use it, and which public commands are available.
---

This page introduces RecordingMode from a public server-docs point of view. It focuses on what the feature does in-game, how to get started when you have access, and which commands are useful.

## What It Does

Recording mode helps players stream or record without private messages, requests, tips, or map visibility getting in the way. Turn it on before going live, then turn it off when you are done.

## How Players Use It

Start with /recording. The sections below explain what RecordingMode is for, which commands players can try, and what to expect when your account has access.

Available features include:

- A personal recording mode that temporarily quiets selected private messages, requests, tips, and visibility surfaces.
- Per-channel settings so players can decide what remains enabled while recording or streaming.
- Automatic capture and restoration of the previous server message and request settings.
- Persistent personal preferences in shared playerdata.
- A clickable status panel showing the current recording state and each channel preference.
- Simple on, off, toggle, status, and per-channel commands.

## Quick Start

1. Start with `/recording` to open or view RecordingMode.
2. Use `/recording status` to check your current progress, settings, or state.
3. Use `/recording help` when you want the in-game help or a short explanation.
4. Try `/recording set msg on` when you are ready to use one of the feature actions.

## Commands

<table class="command-table plugin-command-table">
  <thead>
    <tr>
      <th>Command</th>
      <th>What it does</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>/recording</code></td>
      <td>Opens or shows the main RecordingMode player view.</td>
      <td><code>/recording</code></td>
    </tr>
    <tr>
      <td><code>/recording status</code></td>
      <td>Shows your current RecordingMode status.</td>
      <td><code>/recording status</code></td>
    </tr>
    <tr>
      <td><code>/recording help</code></td>
      <td>Shows the RecordingMode commands available to you.</td>
      <td><code>/recording help</code></td>
    </tr>
    <tr>
      <td><code>/recording set msg &lt;on|<wbr />off&gt;</code></td>
      <td>Sets one of your RecordingMode preferences.</td>
      <td><code>/recording set msg on</code></td>
    </tr>
    <tr>
      <td><code>/recording set tpa &lt;on|<wbr />off&gt;</code></td>
      <td>Sets one of your RecordingMode preferences.</td>
      <td><code>/recording set tpa on</code></td>
    </tr>
    <tr>
      <td><code>/recording set money &lt;on|<wbr />off&gt;</code></td>
      <td>Sets one of your RecordingMode preferences.</td>
      <td><code>/recording set money on</code></td>
    </tr>
    <tr>
      <td><code>/recording set staffmsgs &lt;on|<wbr />off&gt;</code></td>
      <td>Sets one of your RecordingMode preferences.</td>
      <td><code>/recording set staffmsgs on</code></td>
    </tr>
    <tr>
      <td><code>/recording set notablemsgs &lt;on|<wbr />off&gt;</code></td>
      <td>Sets one of your RecordingMode preferences.</td>
      <td><code>/recording set notablemsgs on</code></td>
    </tr>
    <tr>
      <td><code>/recording set tips &lt;on|<wbr />off&gt;</code></td>
      <td>Sets one of your RecordingMode preferences.</td>
      <td><code>/recording set tips on</code></td>
    </tr>
    <tr>
      <td><code>/recording set map &lt;on|<wbr />off&gt;</code></td>
      <td>Sets one of your RecordingMode preferences.</td>
      <td><code>/recording set map on</code></td>
    </tr>
    <tr>
      <td><code>/recording reset</code></td>
      <td>Manages one of your RecordingMode entries.</td>
      <td><code>/recording reset</code></td>
    </tr>
  </tbody>
</table>

## Examples

- No separate examples are listed yet. Use the command examples above as a starting point.

## Good To Know

- Some commands may be hidden until your group, event progress, or unlocks allow them.
- If a command opens a menu, follow the buttons in-game; the website is only the learning guide.
- Settings commands only change your own player preferences unless the page says otherwise.

## Full Reference

The [full synced RecordingMode reference](https://github.com/mrfdev/1MB-Plugins-Docs/blob/main/project-docs/cmi-api/docs/plugins/recordingmode.md) is available for exact technical details.
