---
title: 1MBStaffMsg Guide
description: Learn what 1MBStaffMsg does, how staff use it, and which public staff commands are available.
---

This page introduces 1MBStaffMsg as a public staff guide. It focuses on what the tool does, how authorized staff get started, and which commands are useful.

## What It Does

Runtime recent history helper for server staffmsg and /s recent.

## How Staff Use It

Start with /s recent. The sections below explain what 1MBStaffMsg is for, which commands authorized staff can use, and what its output means.

Available features include:

- Captures staff messages sent through /s &lt;message&gt;, /staffmsg &lt;message&gt;, and /cmi staffmsg &lt;message&gt;.
- Adds /s recent [page], /staffmsg recent [page], /cmi staffmsg recent [page], and /1mbstaffmsg recent [page].
- Adds recent as a tab suggestion for /s, /staffmsg, and /cmi staffmsg when the sender can use CMI staffmsg.
- Uses CMI's existing cmi.command.staffmsg permission by default, so the same staff members who can use staffmsg can view staffmsg recent history.
- Stores recent messages only in memory. Restarting or reloading clears the runtime list.
- Recent rows are compact. Hover over the sender for world, coordinates, time, source command, and online staff recipients.
- Clicking a recent sender suggests /s for a quick staffmsg reply.
- Shift-clicking a recent sender inserts /cmi tppos &lt;x&gt; &lt;y&gt; &lt;z&gt; &lt;world&gt; for fast staff teleport follow-up.

## Quick Start

1. Start with `/s recent` to open or view 1MBStaffMsg.
2. Use `/1mbstaffmsg status` to check your current progress, settings, or state.
3. Use `/1mbstaffmsg info` when you want the in-game help or a short explanation.
4. Try `/staffmsg recent` when you are ready to use one of the feature actions.

## Commands

<table class="command-table plugin-command-table">
  <thead>
    <tr>
      <th>Command</th>
      <th>What it does</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code>/s recent [page]</code></td>
      <td>Runs the 1MBStaffMsg recent action.</td>
      <td><code>/s recent</code></td>
    </tr>
    <tr>
      <td><code>/staffmsg recent [page]</code></td>
      <td>Runs the 1MBStaffMsg recent action.</td>
      <td><code>/staffmsg recent</code></td>
    </tr>
    <tr>
      <td><code>/cmi staffmsg recent [page]</code></td>
      <td>Runs the 1MBStaffMsg staffmsg action.</td>
      <td><code>/cmi staffmsg recent</code></td>
    </tr>
    <tr>
      <td><code>/1mbstaffmsg recent [page]</code></td>
      <td>Runs the 1MBStaffMsg recent action.</td>
      <td><code>/1mbstaffmsg recent</code></td>
    </tr>
    <tr>
      <td><code>/1mbstaffmsg status</code></td>
      <td>Shows your current 1MBStaffMsg status.</td>
      <td><code>/1mbstaffmsg status</code></td>
    </tr>
    <tr>
      <td><code>/1mbstaffmsg info</code></td>
      <td>Shows a friendly explanation of what 1MBStaffMsg does.</td>
      <td><code>/1mbstaffmsg info</code></td>
    </tr>
    <tr>
      <td><code>/1mbstaffmsg help</code></td>
      <td>Shows the 1MBStaffMsg commands available to you.</td>
      <td><code>/1mbstaffmsg help</code></td>
    </tr>
  </tbody>
</table>

## Examples

- `/s this is a staff message through the existing CMI alias`
- `/s recent`
- `/s recent 2`
- `/1mbstaffmsg status`

## Good To Know

- This is a permission-gated staff or server-management feature, not a normal player feature.
- Review the full reference before changing settings or using any command that can write data.

## Full Reference

The [full synced 1MBStaffMsg reference](https://github.com/mrfdev/1MB-Plugins-Docs/blob/main/project-docs/docs/plugins/staffmsg.md) is available for exact technical details.
