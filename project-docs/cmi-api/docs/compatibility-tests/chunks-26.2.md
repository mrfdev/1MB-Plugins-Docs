# Chunks compatibility evidence for Paper 26.2

Checked on 2026-09-12. Chunks changes the server-side view distance used to load and send chunk data around an individual player. It does not change simulation distance. The client still controls its own render distance; cached terrain from Bobby or Distant Horizons is outside this server setting.

## Scope and status

The dependency versions, installed command/API behavior, and live configuration below were verified before implementation. These observations establish the integration contract; they do not claim that player gameplay acceptance has already passed. Record the completed build, automated checks, and local server smoke results below as those gates finish.

The live server was inspected only through read-only SSH. No live configuration, permissions, JARs, world data, or running process was modified.

## Exact dependency baseline

| Component | Local test version after dependency preparation | Evidence |
| --- | --- | --- |
| Paper | 26.2 STABLE build 123 | Fresh [Paper build metadata](https://fill.papermc.io/v3/projects/paper/versions/26.2/builds); latest build published 2026-09-09; maintained JAR and PaperScript state agree. |
| Paper API | `26.2.build.123-stable` | Repository coordinate and [26.2 API Javadocs](https://jd.papermc.io/paper/26.2/org/bukkit/entity/Player.html), whose page identifies build 123. |
| CMI | 9.8.9.10 | Installed `plugin.yml` and fresh [Spigot official CMI version endpoint](https://api.spigotmc.org/legacy/update.php?resource=3742). |
| CMILib | 1.5.9.9 | Installed JAR and fresh [Spigot official CMILib version endpoint](https://api.spigotmc.org/legacy/update.php?resource=87610). |
| LuckPerms | 5.5.82 | Fresh [official metadata](https://metadata.luckperms.net/data/all) and downloaded JAR descriptor. This release increases the cleanup delay for users mid-login. |
| Java target | 25 | Repository `javaTarget`. |

| Local artifact | SHA-256 |
| --- | --- |
| `servers/Paper-26.2/Paper-26.2.jar` | `7b7b3b43c009103e1971a0576c26f655a7dd9b56a0a2a4438e352c03a7fecd08` |
| `servers/Paper-26.2/plugins/CMI-9.8.9.10.jar` | `23ec81d2f29e9a7743cc0060b007c95d568b344d843dc39797ddb0c757ecb03a` |
| `servers/Paper-26.2/plugins/CMILib1.5.9.9.jar` | `6d3d3d73d04a1e70987bd899198971af6bbb5b34550e621f2df8a9cb7bee9862` |
| `servers/Paper-26.2/plugins/LuckPerms-Bukkit-5.5.82.jar` | `46cd6874ad28a1a8383982f1c1395734e79cfee4c814e0d04af84dbd72375e68` |

LuckPerms 5.5.82 was downloaded from the URL returned by its [official metadata](https://metadata.luckperms.net/data/all): [Bukkit release 1669](https://download.luckperms.net/1669/bukkit/loader/LuckPerms-Bukkit-5.5.82.jar). ZIP integrity and its `LuckPerms` name, `5.5.82` version, and Bukkit loader entry point were checked before replacing the local JAR while the maintained server was stopped. The recorded LuckPerms digest is locally computed; the metadata response does not supply a publisher checksum.

The former 5.5.81 JAR is retained at `servers/Paper-26.2/plugins-disabled/dependency-backups/2026-09-12-chunks/LuckPerms-Bukkit-5.5.81.jar`, SHA-256 `27e0030113bad0efc09ef75818e73573f6bcec2b1cc72f64000bc42160113918`. Paper, CMI, and CMILib already matched the latest release for this target and were not replaced during dependency preparation.

## CMI command contract

The relevant installed CMI 9.8.9.10 classes were inspected using `javap` to verify the command path against the actual local dependency, because the public CMI permissions page still includes older `viewrange` terminology.

- Chunks must dispatch a fixed console command: `cmi viewdistance <configured value or -1> <actual online player name> view`. Values exposed by Chunks are restricted to the configured whitelist within 4 through 16.
- The explicit final `view` argument selects `CMIViewDistance.setView(Player, int)`, which invokes only `Player.setViewDistance(int)`. The `simulation` branch invokes a separate API and must never be selected.
- Recipient names are validated before any permission change. Exact case-insensitive CMI control tokens (`reset`, `update`, `view`, `simulation`, and `-s`) are rejected even if they are real usernames: CMI scans those tokens before resolving the recipient. The plugin also verifies that CMI owns `/cmi`. These checks are essential to the view-only, single-player guarantee.
- On Paper 26.2, `-1` passes through to the per-player view-distance API, restoring inherited world defaults. CMI's older-server workaround for versions through 1.21.5 does not apply here. CMI's `reset` token also translates to `-1`, but the numeric fixed form avoids extra parsing paths.
- Reset checks that the current world default is within 4–16 before dispatch. An out-of-range world configuration is reported to staff; the plugin does not silently clamp the default or provide an administrator bypass.
- Never omit the recipient: this command can otherwise operate on the server-wide default. The command also checks `CMIWorld.getWorld(recipient)` before it checks players, so a username matching a loaded world or CMI world alias must fail preflight. Use the public `net.Zrips.CMILib.Container.CMIWorld.getWorld(String)` lookup to mirror that decision.
- CMI's string player lookup checks the exact online player name first. It does not parse a UUID in this command path. CMI receives the validated authoritative `Player.getName()`; LuckPerms receives `Player.getUniqueId()` through its API. Never substitute a nickname, remove a Floodgate prefix, or fall back to a name for LuckPerms.
- The CMI command returns success even when it does not find a matching recipient. A successful command dispatch is therefore not proof of application. Read back `Player.getViewDistance()` and ensure the simulation distance did not change before reporting success.
- The CMI player view setter clamps its own input to -1 through 32 and converts 0 or 1 to 2. Those loose CMI limits are not the Chunks policy; reject an unavailable or out-of-range request before dispatch instead of relying on CMI clamping.

The installed Paper 26.2 build 123 implementation separates load/view, send, and tick/simulation state. The player view setter updates the load-view holder and preserves the `-1` sentinel. Its simulation setter updates a different tick-view field. [Paper's player API](https://jd.papermc.io/paper/26.2/org/bukkit/entity/Player.html#setViewDistance(int)) documents the separate getters/setters; the deprecated no-tick setter is not needed.

## Permission persistence and cache behavior

CMI enumerates positive `cmi.viewdistance.<number>` entries in its provider's effective permission map and chooses their maximum numeric suffix, not the most recently assigned permission. The installed LuckPerms provider supplies its literal contextual permission map. CMI does not expand wildcard-aware access checks across every possible numeric node, so `*`, `cmi.*`, and `cmi.viewdistance.*` do not themselves select 64 chunks. The implementation parses numeric suffixes as doubles before converting its maximum to an integer. Extra inherited, contextual, malformed, or legacy numeric nodes need explicit validation; changing one positive permission without removing or rejecting real numeric conflicts can reapply a different distance later.

A follow-up probe against the actual installed **CMI 9.8.9.10 JAR** passed **nine parser cases**: each of `*`, `cmi.*`, and `cmi.viewdistance.*` with a concrete 4 selected **4**, and each wildcard alone selected **-1**; a positive concrete 16 beat 4; a false 16 did not override 4; and CMI parsed a `12.5` suffix as 12. The probe made zero permission-check calls, permission mutations, or simulation calls. Chunks still rejects unsupported non-integer node forms instead of adopting CMI's permissive fractional parsing. This is actual-CMI integration evidence, not a real-client gameplay test. Evidence is retained in `.scratch/chunks-cmi-wildcard-probe/report.txt`.

CMI 9.8.9.10 reapplies this permission-derived value on player join and schedules a refresh 10 ticks after a world change. If no positive range above 1 remains, it applies `-1`.

The numeric permission cache normally waits 1000 ms before recalculation. Repeated cached reads also refresh its last-read timestamp, so waiting an arbitrary second is not a reliable completion test. The supported public API provides `CMI.getInstance().getPermissionsManager().removeFromCache(uuid)` and forced recalculation through `getPermissionInfo(player, "cmi.viewdistance", true)`. `CMIPerm.viewdistance_$1.getPermissionInfo(player).getMaxValue(0)` reads the same maximum used by CMI's automatic refresh.

LuckPerms persistence must finish before success is reported. Keep a UUID-scoped change pending until the saved nodes, concrete numeric entries in the contextual permission map, CMI's refreshed view-distance permission, and the actual player view distance agree. Wildcard-aware access-check maxima are diagnostic only and are labelled separately from the numeric preference. Require a cooldown plus one pending change per player; a timer alone does not establish that permission propagation or storage succeeded. World changes, reconnects, disable/restart, stale menu clicks, and failed saves require recovery checks.

## Live server configuration, read only

The authorized live Minecraft server was inspected over SSH. Paths below are relative to its server root; machine-specific locations are omitted from published documentation.

| File / scope | Observed distance setting |
| --- | --- |
| `server.properties` | `view-distance=8`, `simulation-distance=4`. |
| `spigot.yml` | `world-settings.default.view-distance: default`, `world-settings.default.simulation-distance: default`; no additional distance overrides matched in this file. |
| `bukkit.yml` | No view/simulation/send-distance override found. |
| `config/paper-global.yml` | `chunk-loading.autoconfig-send-distance: true`; retained `chunk-loading-advanced.auto-config-send-distance: true` also exists. This records both spellings without treating a retained legacy setting as an independently active override. |
| `config/paper-world-defaults.yml` | No view/simulation/send-distance override found. |
| `spawn/dimensions/minecraft/*/paper-world.yml` | No view/simulation/send-distance override found, including overworld, wild, builders, gametype, and event world files. |
| `plugins/Multiverse-Core/worlds.yml` | No matching view/simulation-distance property found. |
| Active dependency filenames | CMI 9.8.9.10, CMILib 1.5.9.9, LuckPerms 5.5.81. Live LuckPerms was not updated. |

Paper 26.2 stores active dimension configuration at `world/dimensions/<namespace>/<key>/paper-world.yml`, as described by the [official world configuration reference](https://docs.papermc.io/paper/reference/world-configuration/). A direct `spawn/paper-world.yml` does not exist on this live server. Old direct world folders also exist and must not be mistaken for the active dimension layout.

The files imply a normal reset target of view distance 8, with simulation distance 4. This is a configuration observation, not a runtime guarantee: another plugin can change live world settings. Chunks should resolve the current player's world default at action time instead of hardcoding 8. [Paper's Spigot reference](https://docs.papermc.io/paper/reference/spigot-configuration/#world-settings_default_view-distance) explains inheritance from `server.properties`; its [server.properties reference](https://docs.papermc.io/paper/reference/server-properties/#view-distance) describes the server-to-client chunk-data setting.

## Verification and manual acceptance

Local verification on 2026-09-12 completed with canonical **build 652**, **65 synchronized JARs**, and **1,619 automated tests** (zero failures/errors, one existing skip), including **46 Chunks tests**. The final Paper inventory probe passed **176 assertions** covering the five choices, actual footer items and effective glow, input cancellation, stale/forged sessions, permission loss and close/quit/world invalidation. Synthetic players intentionally could not enter the real online-player registry, save LuckPerms users, or dispatch CMI. Real Java/Bedrock client acceptance remains required.

Console checks verified out-of-range rejection, adding More 14, changing its shortcut default, removing it, restoring the original presets, reload, and disable/re-enable during a preset write. The saved catalog and active choices agreed after reactivation and after a clean restart. Final health reported all required dependencies enabled, preset validation OK, and no pending operations or reported problems. The local feature is enabled; ordinary players still need `onembcmi.chunks.use`.

Both local runtime starts used Paper 26.2 build 123 and the maintained launcher's required Java **26.0.2.1**. Gradle compiled and tested the artifacts with Java 25. The first probe incorrectly read an optional ItemMeta patch instead of the Nether Star's effective default glint; its corrected data-component assertion passed without production changes. An early console test queued during startup was retried after server readiness. The final server shutdown completed at **15:50 CEST**; the temporary probe was removed and all 65 managed JAR hashes were unchanged. Local evidence is retained under `.scratch/chunks-runtime/` and `.scratch/chunks-build-all.log`.

The footer placement correction on 2026-09-12 moved the glowing **Back to /menu** Nether Star to slot **52**, immediately left of the close barrier at **53**. The player head remains at **45** and the help book at **49**. After a clean shutdown at **15:57 CEST** with all worlds saved, canonical **build 653** passed the complete `scripts/build-all.sh` suite gate and synchronized all **65 JARs** for Paper 26.2 stable build 123. No new runtime probe was run for build 653; the **176 runtime assertions above remain evidence for build 652**. Real-client acceptance of the corrected footer remains required.

Canonical **build 654** adds detailed conflict logging and `/chunks admin inspect <player|uuid> [page]`. The full suite passed **1,630 automated tests** (zero failures/errors, one existing skip), including **57 Chunks tests**, and synchronized **65 JARs** whose descriptors report `1.0.3-654`. Diagnostics regressions verify evidence captured before rollback, inherited wildcard origin and maximum, contextual/temporary denial details, read-only inspection, retention after logout and later success, ambiguous-name rejection without external lookup, bounded/sanitized snapshots, and repeated-log suppression with complete latest evidence. These automated checks do not replace real Java/Bedrock client acceptance or retroactively create a failure snapshot for an older build.

Build 654's guard incorrectly treated a wildcard-expanded access-check maximum of 64 as a selected numeric distance, causing false conflicts for staff with broad permissions. Its wildcard diagnostics results above document that historical behavior. The verified CMI distinction described in this page corrects that interpretation within Chunks; it does not require changes to staff global LuckPerms grants or introduce a preset-limit bypass.

The build 654 local Paper smoke reached readiness at **16:09:23 CEST**. Console inspection accepted a canonical UUID with an optional page and correctly reported an empty retained history and offline current state; missing arguments and an unknown exact name were rejected. Health was ready with zero pending operations and zero reported problems, and debug command metadata listed inspection with its admin permission. Console uses the shared all-rows renderer, so this check does not establish in-game pagination or an online player's conflict behavior; wildcard conflicts and retention were covered by the automated tests above. Shutdown completed cleanly at **16:09:45 CEST**, all worlds were saved, and the process exited with code 0. Evidence is retained in `.scratch/chunks-diagnostics-runtime.log`.

Canonical **build 655** corrects the false staff-wildcard conflict inside Chunks without editing global LuckPerms grants or adding an administrator bypass. The full suite passed **1,633 automated tests** (zero failures/errors, one existing skip), including **60 Chunks tests**, and all **65 synchronized JARs** were verified as `1.0.3-655`. The corrected guard uses the concrete numeric permission-map maximum; wildcard-aware access checks remain separately labelled diagnostics. Verification combines those tests with the **nine actual-CMI parser cases** described above. Paper remained 26.2 stable build 123 and the maintained server was left stopped at the end of that verification. The automated verification did not add a server startup or controlled real-client test for build 655; prior runtime evidence remains attributed to its original build.

After build 655's wildcard correction, the owner reported that the feature seemed to work in gameplay. This is recorded as positive initial user feedback, not completion of the full Java/Bedrock acceptance matrix: client editions, every preset/reset/reconnect path, and the adversarial cases below were not individually reported as verified.

The [live Paper documentation index](https://docs.papermc.io/llms.txt) was used to discover the current references. The [custom InventoryHolder guide](https://docs.papermc.io/paper/dev/custom-inventory-holder/) supports authoritative inventory identity, and [Paper's scheduler guide](https://docs.papermc.io/paper/dev/scheduler/) requires world/player operations on the server thread while expensive storage work runs asynchronously.

The live selected-state GUI follow-up on **2026-09-14** passed canonical **build 661** with **65 synchronized JARs** and **1,701 tests**, zero failures/errors and one existing WorthDrift fixture skip. The **73 Chunks tests** include saved-versus-confirmed state, Reset versus equal numeric choices, cooldown, runtime drift, slow persistence, unreadable data, and both immediate and delayed confirmation of a restored preference. The shared library's **274 tests** include seven new in-place GUI refresh regressions for identity, item cloning, invalid batches, unchanged displays, queued actions, debounce, and closing/navigation behavior.

On the maintained Paper **26.2-123** server, the temporary `1MB-ChunksLiveGuiProbe` passed **347 assertions** using real Paper inventories and ItemStacks. This verified effective selected glint, actual names/lore for saving/confirming/cooldown/Reset/unavailable states, the status item at 31, the existing footer, in-place item updates, unchanged session identity, queued action preservation, invalid input, and close/navigation paths. The successful run used **CMI 9.8.9.11**, **CMILib 1.5.9.9**, and **LuckPerms 5.5.82**. Health reported ready with zero pending UUIDs or reported problems. Startup completed at **11:42:10 CEST**, the probe passed at **11:42:24**, and shutdown completed at **11:42:48** with exit code 0. All 65 managed JAR hashes and both Chunks YAML hashes were unchanged after the smoke checks; the temporary probe JAR/data were removed. Evidence: `.scratch/chunks-live-gui/` and `.scratch/chunks-live-gui-build-all.log`.

Two earlier probe attempts exposed test-fixture mistakes: expecting a neutral status for a synthetic player with no loaded LuckPerms user, and using a synthetic username longer than Paper's 16-character profile limit. Both were corrected in scratch code; no production changes were required. The final probe deliberately cannot create/load LuckPerms users or pass online-player identity checks. Injected presentation snapshots verify rendering, while automated coordinator tests verify transaction state. These checks do not establish real Java/Bedrock packet rendering, actual player CMI delivery, or gameplay acceptance of the live menu.

The preset-removal preview follow-up on **2026-09-14** passed canonical **build 662** with **65 synchronized JARs**, all reporting `1.0.3-662`, and **1,713 automated tests**, zero failures/errors and one existing WorthDrift fixture skip. The **85 Chunks tests** include 12 new policy, report, and coordinator regressions: stable saved preferences versus matching unmanaged/Reset distances; valid and blocked current-world defaults; pending/recovery and unreadable data; UUID attribution for Java, renamed, and Bedrock names; immutable and sanitized reports; shortcut replacement and empty groups; stale/wrong-group input; fresh world/online snapshots; and no saves, application, scheduling, or offline lookup from preview.

The build 662 local Paper console smoke passed **19 checks**. Namespaced previews of More 16 and Less 4 showed the proposed catalog and shortcut changes, explicitly reported zero online players and unknown offline impact, and preserved the active choices. Wrong-group, unlisted, out-of-range, missing, extra, and invalid-page arguments were rejected. Debug metadata exposed the preview command with `onembcmi.chunks.admin`; health remained ready with zero pending UUIDs and zero reported problems. The server used **Paper 26.2-123**, **CMI 9.8.9.11**, **CMILib 1.5.9.9**, and **LuckPerms 5.5.82**, reached readiness at **12:04:47 CEST**, and stopped cleanly at **12:05:20** with exit code 0. All **67 protected hashes** (65 managed JARs and both Chunks YAML files) were unchanged. Evidence: `.scratch/chunks-removal-preview/` and `.scratch/chunks-removal-preview-build-all.log`.

That console smoke used no online players and performed no actual removal. Console intentionally renders every report row, so passing an optional page does not establish in-game pagination or ordinary-player permission denial. Automated tests cover populated snapshots and non-mutation; real staff/client acceptance remains below. A preview's in-range reset target is not a guarantee of subsequent LuckPerms/CMI delivery, and each page invocation takes a fresh snapshot.

The player support-reference follow-up on **2026-09-14** passed canonical **build 663**, with all **65 managed JARs** reporting `1.0.3-663` and **1,728 automated tests**, zero failures/errors and one existing WorthDrift fixture skip. The **100 Chunks tests** include 15 new regressions for exact player/log references, immediate/delayed restoration, unresolved recovery, distinct repeated incidents, historical/current separation after renaming and success, retention bounds, startup merging, real-file restart round trips, coalesced writes, failed loads/saves, damaged/oversized/unsupported files, and recording/inspection while background I/O is blocked.

A disposable `1MB-ChunksSupportProbe` seeded two clearly labelled diagnostic fixtures for an unregistered UUID, using the real asynchronous history writer. After a clean restart, **27 runtime assertions** passed for exact reference retention, original Bedrock-prefixed and renamed labels, permission evidence, separate offline/current state, staff command pagination and navigation payloads, public permission denial without evidence disclosure, own-reference Status output, tab completion, and read-only behavior. Namespaced console commands also accepted the correct reference and history UUID, rejected a player UUID used as an incident reference, rejected shortened/missing/invalid-page input, and exposed both commands in debug metadata. No LuckPerms user was created or loaded, and no view or simulation distance was applied by the fixture.

The first start reached readiness at **12:32:23 CEST** and stopped at **12:33:21**. An initial scratch-probe assertion used an old Adventure 4.x click-event accessor because the test helper included an older cached dependency; the probe was corrected to compile against installed **Adventure 5.2.0** and inspect its typed payload. This required no production change. The second start reached readiness at **12:34:41**, passed all 27 assertions at **12:35:07**, and stopped cleanly at **12:35:47** with exit code 0. Both runs used **Paper 26.2-123**, **CMI 9.8.9.11**, **CMILib 1.5.9.9**, and **LuckPerms 5.5.82**. Both references and the 362-byte fixture history were unchanged across restart and read-only commands. All **67 protected hashes** (65 managed JARs and both Chunks YAML files) matched afterward; the temporary probe and fixture history were removed, restoring the history file's original absence. Evidence: `.scratch/chunks-support/` and `.scratch/chunks-support-build-all.log`.

## Optional visual explanation trial, proposal 9

Canonical **build 665** on **2026-09-14** synchronized all **65 JARs** at `1.0.3-665` and passed **1,739 tests**, zero failures/errors and one existing WorthDrift fixture skip. The **111 Chunks tests** include eight new diagram regressions for expanding/symmetric terrain, bounded intermediate values, sparse/empty catalogs, end stops, Reset identity/current-world semantics, invalid world defaults, and retired/unlisted choices. Three new head regressions cover missing/mismatched profiles and available Java/Bedrock skins with authoritative UUIDs and no profile-completion calls.

The prototype is disabled by default. `/chunks explain` opens the illustration and its main-menu spyglass appears only when `prototype.distance-explanation-enabled` is enabled by staff; the normal public use permission remains sufficient. Arrows change a coarse centre-player illustration, not a saved preference. The illustration has no CMI/LP write or chunk-loading path. Exact preset labels and current-world Reset remain separate from the schematic area, which makes no rendering or performance claims. The owner keep/remove decision remains open.

The live [Paper documentation index](https://docs.papermc.io/llms.txt) led to the [custom holder guide](https://docs.papermc.io/paper/dev/custom-inventory-holder/) and exact **26.2.build.123-stable** [click](https://jd.papermc.io/paper/26.2/org/bukkit/event/inventory/InventoryClickEvent.html) and [drag](https://jd.papermc.io/paper/26.2/org/bukkit/event/inventory/InventoryDragEvent.html) contracts. Navigation uses the existing shared next-tick action dispatch and owner/session/nonce guards. [SkullMeta](https://jd.papermc.io/paper/26.2/org/bukkit/inventory/meta/SkullMeta.html) and [PlayerProfile](https://jd.papermc.io/paper/26.2/com/destroystokyo/paper/profile/PlayerProfile.html) documentation, plus inspection of the installed Paper build, verified that applying skull metadata with empty textures can trigger asynchronous profile completion. An initial **build 664** runtime trial passed 111 menu assertions but exposed that lookup for the synthetic UUID. Chunks now attaches only an available skin matching the player's server UUID; otherwise it uses an ownerless generic head in both menus. This correction is local to Chunks and does not alter the shared GUI helper or other plugins.

The final disposable Paper probe passed **114 assertions** at **13:02:18 CEST**, including actual six-row inventory layout, terrain expansion, valid skin retention, ownerless missing-skin heads, staff switch permissions, hidden/off command and tab behavior, no custom arguments, unchanged main presets, pending-click debounce, every invalid click type, creative insertion, drag, forged owner/stale views, permission/generation/world-default/world-UUID changes, Reset illustration, and all footer/close/quit/kick/world-change paths. No LuckPerms user was loaded or created, no preference was pending, and no view/simulation setter or CMI command was invoked. The final run emitted no profile-fetch warning. These are real Paper inventory checks with synthetic players, not client-packet or gameplay acceptance.

Final runtime: **Paper 26.2-123**, **CMI 9.8.9.11**, **CMILib 1.5.9.9**, **LuckPerms 5.5.82**, launcher Java **26.0.2.1** (compile/test target Java 25). Startup completed **13:01:56 CEST**; health was ready with zero pending/problems and the trial disabled; clean shutdown completed **13:03:00** with exit code 0. All **67 protected hashes** matched after the final probe. The only initial config change was normal default repair adding the false trial key and its comments; toggling on/off preserved that repaired file exactly. No support history was created, and the probe JAR/data were removed. Evidence: `.scratch/chunks-visual/` and `.scratch/chunks-visual-build-all.log`. Java/Bedrock gameplay and the explicit owner keep/remove decision remain in `checklist.md`; no live deployment or public documentation publication is claimed.

## Optional connection advice, proposal 6

Canonical **build 666** on **2026-09-14** synchronized all **65 JARs** at `1.0.3-666` and passed **1,753 automated tests**, zero failures/errors and one existing WorthDrift fixture skip. The **125 Chunks tests** include 14 new regressions for warmup and sample spacing, isolated/interrupted spikes, threshold boundaries, invalid/stale estimates, clock rollback, UUID isolation/session removal, enabled-only lower choices, sparse/retired catalogs, minimum/unknown distances, readiness gates, and conditional language.

The live [Paper documentation index](https://docs.papermc.io/llms.txt), [scheduler guide](https://docs.papermc.io/paper/dev/scheduler/), [custom inventory holder guide](https://docs.papermc.io/paper/dev/custom-inventory-holder/), and exact **26.2.build.123-stable** [`Player#getPing()` contract](https://jd.papermc.io/paper/26.2/org/bukkit/entity/Player.html#getPing()) were consulted. The implementation observes the existing application-layer estimate on the main thread, at most every ten seconds after a 30-second warmup. Three consecutive elevated observations may offer a currently enabled lower preset. It sends no custom ping packets, measures no bandwidth/FPS, makes no lag diagnosis, and acknowledges Bedrock/proxy uncertainty. Low/unavailable readings are not labelled healthy. Reading advice never changes a preference.

The disposable `1MB-ChunksConnectionProbe` passed **99 runtime assertions** at **13:27:01 CEST** using real Paper inventories and synthetic Java/Bedrock-prefixed players with time-seeded estimates. Checks covered global off/on behavior, staff-only configuration, existing player permission, text pagination/input validation, independent diagram switch, no warmup/repeated-call ping reads, main-thread observations, current preset retirement, pending/cooldown/problem/unreadable gates, actual advice item and footer rendering, all rejected click types, creative/drag/forged-owner input, double-click admission, stale/replaced/closed/world-changed sessions, lost access, logout/kick, invalid threshold isolation and health output, reload, dormancy/reactivation, and disabling an already queued report. No LP user was loaded or created, no preference was pending, and no command or view/simulation setter was invoked for a probe player. No profile-fetch warning was emitted.

Runtime versions: **Paper 26.2-123**, **CMI 9.8.9.11**, **CMILib 1.5.9.9**, **LuckPerms 5.5.82**, launcher Java **26.0.2.1** (compile/test target Java 25). A first normal startup repaired the existing config by adding only the disabled advice flag, 250-ms threshold, and their comments; an initial scratch staging check was corrected to recognize the actual `1.0.3-666` descriptor format. The probe run reached readiness at **13:26:41**, health reported ready with zero pending/problems, and clean shutdown completed **13:27:34 CEST** with exit code 0. All **67 protected hashes** matched after staff toggle restoration and shutdown. The preset file remained unchanged; no support history was created, and the probe JAR/data were removed. Advice and the visual trial remain off. Evidence: `.scratch/chunks-connection/` and `.scratch/chunks-connection-build-all.log`. These checks do not measure a real connection or replace mobile/Java/Bedrock gameplay acceptance in `checklist.md`.

## Help me choose and short preset explanations, proposal 4

Canonical **build 667** on **2026-09-14** synchronized all **65 JARs** at `1.0.3-667` and passed **1,763 automated tests**, zero failures/errors and one existing WorthDrift fixture skip. The **135 Chunks tests** include ten new regressions for the guide's current/retired/empty catalogs, every intermediate preset, numeric guidance independent of staff group names, bounded immutable card text, valid/invalid Reset defaults, qualified wording, and preservation of selected/pending/cooldown meaning in the longer preset tooltips.

`/chunks choose` and the main menu's slot-40 writable book open a single page with lower-distance, Reset, and wider-view/screenshot cards. Clicking an available card returns to the normal preset buttons and prints one reminder. It performs no preference application. The main tooltips and `/chunks info` explain the trade-offs without promising smoother play, higher FPS, lower ping, or exact bandwidth savings. Only current enabled values appear; grey cards handle empty ranges and invalid Reset defaults. Both optional connection advice and diagram settings remained off throughout this verification.

The live [Paper documentation index](https://docs.papermc.io/llms.txt), [custom-holder guide](https://docs.papermc.io/paper/dev/custom-inventory-holder/), [scheduler guide](https://docs.papermc.io/paper/dev/scheduler/), and exact **26.2.build.123-stable** [click](https://jd.papermc.io/paper/26.2/org/bukkit/event/inventory/InventoryClickEvent.html) and [drag](https://jd.papermc.io/paper/26.2/org/bukkit/event/inventory/InventoryDragEvent.html) contracts were consulted. The guide reuses the shared owner/session/nonce checks, cancellation, next-tick dispatch and debounce, with additional readiness, permission, catalog-generation and world/default validation. It adds no timer, network probe, persistent state, or player identity lookup.

The final disposable `1MB-ChunksChooseProbe` passed **119 Paper assertions** at **13:47:51 CEST**. Coverage included command permission/completion and invalid arguments; main/guide layouts and footers; available/missing-skin heads; Java/Bedrock-prefixed text; all invalid click types, creative/drag/forged-owner input; rapid card clicks returning only once; retained original preset controls; stale/replaced sessions; permission/world/default/catalog changes; retired/empty/all-intermediate catalogs and dynamic text guidance; unsafe Reset; footer navigation; close/quit/kick/world-change invalidation; actual reload; and dormancy/reactivation. No LP identity was loaded or created, no preference operation or view/simulation setter ran, and no ping was read. The only player command was the explicit `/menu` footer action. These were real Paper inventories with synthetic players, not client-packet or gameplay acceptance.

The first probe reached 116 assertions before Paper rejected its attempt to use a synthetic player as a vanilla command listener. Only the scratch adapter was changed to execute the registered Bukkit command directly; production code and build 667 stayed unchanged. Both runs used **Paper 26.2-123**, **CMI 9.8.9.11**, **CMILib 1.5.9.9**, **LuckPerms 5.5.82**, and launcher Java **26.0.2.1** (compile/test target Java 25). The final run reached readiness at **13:46:58**, reported healthy with zero pending/problems, and stopped cleanly at **13:48:44 CEST** with exit code 0. All **67 protected hashes** matched, including both Chunks YAML files. No history file or profile-fetch warning was produced; the temporary probe JAR/data were removed. Evidence: `.scratch/chunks-choose/`. Real Java/Bedrock readability and owner feedback remain in `checklist.md`; no live deployment or public documentation publication is claimed.

The installed Paper shutdown implementation explicitly exits the JVM, so diagnostic persistence does not claim that unfinished asynchronous writes must survive shutdown. Completed, atomically replaced snapshots survive restart; pending/unavailable history is labelled and the console log remains the fallback. The probe verifies actual Paper command rendering and persistence with synthetic evidence and senders. Automated coordinator tests verify player failure feedback and recovery; real Java/Bedrock client delivery and owner gameplay acceptance remain required.

Before live handoff, test with Java and Floodgate/Bedrock accounts:

1. Every configured button and legacy command, reset, exact persistence after relog, and new-world defaults after reset.
2. Rejection of unconfigured values, values below 4 or above 16, malformed numbers, wrong direction, extra arguments, and forged menu inputs.
3. Rapid clicks/commands, close/reopen, shift-click, hotbar/offhand swap, drag, double-click, drop, creative interactions, full inventory, disconnect, world change, and stale menu sessions.
4. Failed/slow LuckPerms persistence, a concurrent second change, inherited/conflicting view-distance nodes, and recovery after disable/restart between save and apply.
5. Exact same simulation distance before and after every success, rejected input, and recovery path. Verify that other players and world defaults remain unchanged.
6. Client render distance lower than the server preference, cached chunks from Bobby/Distant Horizons, and removal of an option by an admin while a menu or saved preference still refers to it.
7. Preview a preset removal with affected, unaffected, pending, unreadable, and blocked-world players online. Check exact UUIDs, counts, reset targets, page navigation, admin permission denial, and unchanged preferences/menus/distances. Actual removal remains a separate command and a retired choice must fail a later preview.
8. Share a failed change's support reference, retrieve it through Status, and open the exact incident/history as staff after recovery, success, rename, logout, and restart. Verify player/staff permissions, pagination, historical/current labels, retention eviction, and storage-failure recovery without preference or simulation changes.

Run `scripts/build-all.sh` with the maintained Paper server stopped for the complete suite test build. After gameplay acceptance, `gradle stageTestedJarsForLive` stages the exact tested set; do not manually replace live dependencies or publish partial Feature Plugin builds as a tested suite.

## Client-limit advice, proposal 5

Implemented on **2026-09-14** in canonical local suite **build 668**. Preset hover text compares each offered target with the reported client request; Reset uses the exact valid current-world default. Main status and own-head hover text compare only a confirmed preference. `/chunks status [page]` adds a fuller explanation while retaining previous support references. Advice never blocks an offered preset, changes a preference, or reports an unconfirmed save as active.

The live [Paper documentation index](https://docs.papermc.io/llms.txt) led to the [custom-holder](https://docs.papermc.io/paper/dev/custom-inventory-holder/) and [scheduler](https://docs.papermc.io/paper/dev/scheduler/) guidance; the exact **26.2.build.123-stable** [Player API](https://jd.papermc.io/paper/26.2/org/bukkit/entity/Player.html#getClientViewDistance()) and inventory click/drag contracts were verified. Paper documents a fallback of **2** before receiving client settings. The getter cannot distinguish it from an intentional 2, so advice labels it as a possible initial default without asserting a confirmed mismatch. Nonpositive values are unavailable; positive client reports are not clamped to the server preset range. Bedrock/proxy/mod differences, actual rendering, and Bobby/Distant Horizons caches remain distinct. The existing raw placeholder stays compatible.

Canonical `scripts/build-all.sh` completed in **1m 35s**, synchronized all **65 JARs**, and passed **1,773 automated tests** (zero failures/errors, one existing WorthDrift fixture skip), including **145 Chunks tests**. Ten added regressions cover lower/equal/higher reports, the ambiguous fallback and invalid values, exact/invalid Reset defaults, pending/unreadable/recovery/cooldown states, selected glow and action text, fresh reports, and the absence of rendering promises. Focused tests passed before the full suite.

A disposable local probe passed **79 assertions** on real Paper inventories with synthetic Java/Bedrock-prefixed players, supplied client values and presentation states. Checks cover preset/status/head output, in-place refresh without unsolicited chat or reopening, fresh status after deferred clicks, unchanged guide/footer navigation, pagination/support references, permission denial, forged/stale sessions, shift/double/hotbar/offhand/drop/creative/outside/bottom-inventory input, dragging, close/quit/kick/world changes, reload, and dormancy/reactivation. No LP identity was loaded/created; no preference operation, view/simulation write, or ping read occurred. Only the explicitly clicked `/menu` footer dispatched its command. Client reads were asserted to run on the main thread.

Runtime: **Paper 26.2-123**, **CMI 9.8.9.11**, **CMILib 1.5.9.9**, **LuckPerms 5.5.82**, launcher Java **26.0.2.1** (compile/test target Java 25). The probe passed at **14:55:18 CEST**; health was ready with zero pending/problems, and the server stopped cleanly at **14:55:40 CEST**, exit code 0. All **67 protected hashes** matched (65 managed JARs plus Chunks config/presets), no support history file was created, and the probe JAR/data were removed. Connection advice and the visual trial remain off. Evidence: `.scratch/chunks-client-limit/`.

These tests do not measure real client packets, Bedrock slider translation, terrain rendering, or mod caches. Actual Java/Bedrock readability and client-setting changes remain in `checklist.md`. No live deployment or public docs publication is claimed.

## Preference/session controls, proposal 3

Implemented on **2026-09-14** in canonical local suite **build 669**. `/chunks options` and the main repeater open Always / This session, Save this choice and Use my saved preference. The default remains Always. Session mode requires an allowed saved baseline; temporary choices preserve normal LP nodes, persist through world changes, and end on logout/restart/Chunks reload/preset edits/disable. Mode selection alone changes no distance. Saved, session, confirmed and after-relog state are labelled separately. Legacy preset commands follow the selected mode; new commands use the existing player-use permission. The options page preserves the standard right-side star/barrier footer and in-place refresh.

The canonical `scripts/build-all.sh` gate checked the latest stable Paper build within target 26.2 and synchronized all **65 JARs**. **1,794 automated tests** passed with zero failures/errors and one existing WorthDrift fixture skip, including **166 Chunks tests**. New coverage checks durable-versus-transient state, numeric and Reset sessions, explicit promotion, saved restoration, owned-node cleanup versus foreign replacements, conflicts/context/permission ordering, unavailable/changed baselines, UUID targeting, save failure/recovery, mode changes and stale expected modes, cooldown/busy gates, world changes, rename/relog, permission loss, quit/disable, preset retirement, preview impact and confirmation labels. Simulation behavior remains unchanged.

A separate disposable adapter probe passed **33 checks against installed LuckPerms 5.5.82 and CMI 9.8.9.11**. It loaded one fresh synthetic UUID with an accompanying fixture name and used real LP NodeMaps, persistence and cached permission evaluation. Session 4/6/12/Reset/16 preserved the saved 16 normal nodes; both LP and CMI numeric evaluation agreed with each temporary selection. Cleanup, explicit save promotion and a saved Reset baseline also passed. The probe restored and saved that fixture's original normal nodes and removed its owned transient nodes. No real player's permissions were modified. This used fixed global query contexts and a synthetic player; it did not dispatch a CMI view command or perform an external identity lookup. Context conflicts and UUID variants are additionally covered by automated tests.

The final disposable GUI probe passed **89 assertions** on real Paper inventories with synthetic Java/Bedrock-prefixed players and supplied presentation states. It covered new command permissions/completion, invalid mode, both mode glows, current/saved/after-relog labels, cooldown and pending save/restore controls, Reset, exact footer/border placement, dynamic in-place refresh, shift/double/hotbar/offhand/drop/creative/outside/bottom-inventory/drag/forged-owner input, rapid navigation, stale sessions, permission/world/default/catalog changes, close/quit/kick, reload and dormancy/reactivation. It performed no LP identity load or preference/distance/ping operation; only the explicitly clicked `/menu` footer command ran. Initial scratch probe startup and glow-access assertions were corrected without changing the canonical production artifacts. These checks do not substitute for client-packet, gameplay or mobile-readability acceptance.

Runtime: **Paper 26.2-123**, **CMI 9.8.9.11**, **CMILib 1.5.9.9**, **LuckPerms 5.5.82**, launcher Java **26.0.2.1**, compile/test Java **25**. The GUI probe passed at **15:40:52 CEST**; final health was ready with zero pending/problems and zero retained incidents. Shutdown completed at **15:41:17 CEST**, exit code 0. All **67 protected hashes** matched (65 managed JARs and both Chunks YAML files); no support-history file was created, the scratch probe JAR/data were removed, and the optional connection advice and diagram remained off. Evidence: `.scratch/chunks-sessions/`. Real Java/Bedrock session/relog and readable-lore acceptance remains in `checklist.md`. No live deployment is claimed.

## Temporary screenshot mode, proposal 7

Implemented on **2026-09-14**, with the owner's selected **five-minute** default, in final canonical local suite **build 671**. `/chunks screenshot` and the options-page spyglass start the highest enabled preset from 12–16 above the current confirmed distance. `/chunks screenshot stop` and the separate End control return early. Start requires a confirmed allowed choice, a saved baseline, use permission and normal cooldown. The timer begins only after CMI/Paper confirmation, preserves the saved preference, and returns to the previous allowed saved or session choice. Status and two new placeholders expose the phase/countdown. Staff can disable the option and set 30–1800 seconds using the existing in-game config commands.

The final canonical `scripts/build-all.sh` gate verified the latest stable Paper build within **26.2** and synchronized all **65 JARs**. **1,817 automated tests** passed with zero failures/errors and one existing WorthDrift fixture skip, including **189 Chunks tests**. The 23 new tests cover timer boundaries, monotonic time versus wall-clock changes, higher/current/retired/sparse presets, Reset across worlds, previous session restoration, explicit save/restore/replacement, repeated starts, early stop versus automatic expiry during cooldown, world reconciliation at expiry, UUID-preserving rename/relog, quit/reload/disable, lost permission, changed saved data, blocked/retired return targets, failed start preflight, failed runtime confirmation, support references and bounded failure cleanup. The permission adapter and view-only CMI command renderer are unchanged from their prior verification.

A disposable probe passed **137 assertions** on real Paper inventories with synthetic Java/Bedrock-prefixed players and supplied presentation/timer states. It checked new command permissions/completion and invalid input; highest-preset selection and no wider option; five-minute duration text; active/starting/returning labels; distinct timed-return and relog choices; placeholders and monotonic countdown refresh without reopening or unsolicited chat; retained normal controls/footers; click/shift/double/hotbar/offhand/drop/creative/outside/bottom-inventory/drag/forged-owner input; stale/world/default/catalog/access changes; close/quit/kick; reload and dormancy/reactivation. Actual staff config commands changed the duration to 60, rejected starts at invalid 29 without disabling ordinary presets, restored 300, and switched screenshot controls off/on. The final settings are enabled/300 seconds. The probe performed no LP identity load/create, preference save, view/simulation setter or ping read; only explicitly clicked `/menu` commands ran for fixture players. Actual timer transactions are covered by automated service tests, not this synthetic inventory probe.

The first Paper probe exposed the shared tooltip formatter inserting a space into `5:00`; the duration line was fixed and the complete suite rebuilt as **671**. A subsequent scratch-only test needed to close its earlier synthetic menu before beginning an independent permission-denial check; production artifacts were unchanged by that probe adjustment.

Runtime: **Paper 26.2-123**, **CMI 9.8.9.11**, **CMILib 1.5.9.9**, **LuckPerms 5.5.82**, launcher Java **26.0.2.1**, compile/test Java **25**. Final GUI verification passed at **16:06:16 CEST**. Health was ready with zero screenshot sessions, pending UUIDs, reported problems and retained incidents. Shutdown completed at **16:07:04 CEST**, exit code 0. All **67 final protected hashes** matched (65 managed JARs, presets and normalized config); initial config repair added only the new screenshot switch/duration and comments. No support-history file was created; scratch JAR/data were removed. Connection advice and the visual trial remain off. Evidence: `.scratch/chunks-screenshot/`. Real Java/Bedrock gameplay, five-minute return experience and mobile readability remain in `checklist.md`; no live deployment is claimed.
