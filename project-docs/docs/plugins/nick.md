# Nick

Nick owns `/nick` and replaces raw player access to CMI nicknames with a safer picker. Players do not type color codes directly; they pick curated presets, cleanup tools, history entries, or favorites, and the plugin applies the final trusted nickname through CMI from console.

Before enabling this jar, remove or disable CMI's own `/nick` alias/command registration in CMI's alias configuration so this feature can own `/nick`.

## Player Flow

`/nick` opens a hardened GUI with a live preview. The player can browse public colors, gradients, seasonal styles, group styles, economy unlocks, milestone styles, moods, tools, history, favorites, top nicknames, and bio settings.

The main GUI includes a "Turn Off Nickname" button next to the current preview. It uses the same safe clear path as `/nick clear`, dispatching the configured CMI command `cmi nick off {player} -s` while keeping Nick history and favorites intact.

The main GUI also links to Style Lab and Nick Museum when enabled. Style Lab can make the current color lighter, darker, warmer, cooler, redder, greener, bluer, pastel, softer, bolder, or safely random without allowing players to type raw hex codes. Nick Museum tracks styles a player has used as a personal collection; it does not make styles exclusive, so other players can still use the same public presets. `/nick museum top` shows the largest public collections as a fun leaderboard.

The main GUI includes a Quick Undo button. It runs the same safe path as `/nick undo`, restoring the newest previous Nick snapshot during the configured mistake-fix window.

The bottom-left player head on the main GUI shows a quick account snapshot: real name, money when Vault economy is available, EXP level, current Nick preview, previous Nick snapshot, lock state, museum count, and whether a legacy nickname was captured. Clicking it closes the GUI and prints `/nick status`.

The History & Favorites page keeps the two concepts separate. History is a rolling list of previous Nick choices and only starts after there is a previous Nick choice to store; the first successful Nick save has no previous Nick yet. Favorites are permanent saved slots and are labeled separately.

The visible nickname text is strict: only letters, numbers, and underscores are accepted. Raw legacy colors, MiniMessage, hex input, semicolons, pipes, tellraw-style text, command chains, and formatting codes are removed or rejected. The default rule also requires the nickname to stay related to the player's real Minecraft name, so `MyName666` can become `MyName` or `Name`, but unrelated names such as random memes are blocked.

Style presets use trusted CMI color syntax generated by the plugin, such as `{#bde0fe}` and `{#bde0fe>}Name{#ffc8dd<}`. Players cannot type these markers themselves.

`/nick share chat` posts a safe clickable style card in-game. `/nick share discord` is disabled by default, but can use a configured DiscordSRV share-card command so players can show a public style id without exposing raw color syntax.

## Legacy Nickname Migration

When Nick first sees a player, it asks CMI for the player's existing CMI nickname and stores a one-time legacy snapshot in shared playerdata. This lets long-time players see what they had before the safer Nick rules took over.

Legacy restores are limited by `legacy.restore-uses`, defaulting to one restore. A player can restore the captured legacy nickname while uses remain, even if that old nickname would fail the new related-name or style rules. After the restore count reaches zero, the legacy entry remains visible but view-only.

Legacy nicknames are intentionally not full participants in the new perk system. They cannot be shared to chat or Discord, cannot be saved as permanent favorites, cannot be rated, and are excluded from `/nick top`. Any new `/nick set`, style, tool, favorite, or preset use moves the player back into the new controlled Nick workflow.

Admins with `onembcmi.nick.admin` see gray `id: ...` lines at the bottom of GUI item lore for preset ids, page ids, tool ids, favorites, history entries, and legacy actions. This makes it easier to match GUI choices to YAML config keys.

Admins can also use `/nick admin review` as a pending review queue. Blocked, duplicate-looking, and unrelated nickname catches can be stored in `stats.yml` with a stable entry id. In-game it opens a GUI of pending entries; console receives a paged text list. `/nick admin review logs` still shows recent Nick log lines, and `/nick admin review resolve <entry-id> <note>` or `dismiss <entry-id> <note>` closes pending review entries without changing player data.

## Commands

```text
/nick
/nick <name>
/nick set <name>
/nick style <style-id>
/nick clear
/nick undo
/nick status
/nick status <player>
/nick bio <short bio>
/nick rate <player> <1-5>
/nick top
/nick top claim
/nick share chat
/nick share discord
/nick copycat <player>
/nick favorite save <slot>
/nick favorite use <slot>
/nick history
/nick legacy status
/nick legacy restore
/nick tools
/nick lab <lighter|darker|warmer|cooler|redder|greener|bluer|pastel|softer|bolder|random>
/nick milestones
/nick museum
/nick museum top
/nick museum <player>
/nick realname <player>
/nick admin inspect <player>
/nick admin review
/nick admin review logs
/nick admin review resolve <entry-id> <note>
/nick admin review dismiss <entry-id> <note>
/nick admin validate
/nick admin similar <name>
/nick admin reset <player> <reason>
/nick admin reset <player> --lock <minutes> <reason>
/nick admin lock <player> <minutes> <reason>
/nick admin unlock <player>
/nick admin reload
/nick debug
/nick debug all
/nick debug presets
/nick debug presets validate
/nick debug permissions
```

`/nick status <player>` is player-safe and shows the real name, current plain nickname, style, bio, rating, and cooldowns. `/nick realname <player>` dispatches the configured CMI realname command.

`/nick undo` restores the newest previous Nick snapshot during the configured quick undo window. It intentionally gives players a short mistake-fix path without making the long nickname cooldown meaningless.

`/nick top claim` is disabled by default. When enabled after live tuning, the configured top-ranked player can claim one reward per month after meeting the minimum rating count, minimum average, optional current-month rating count, and minimum nickname age. Claims are stored by month in playerdata to prevent repeat claiming.

`/nick admin reset` now requires a reason and can include `--lock <minutes>` to temporarily prevent the player from changing their Nick again. `/nick admin lock` and `/nick admin unlock` manage the same lock without resetting the profile.

`/nick debug` uses the shared 1MB-CMIAPI debug pages, so console and admins can see build metadata, Java/Paper runtime details, hooks, commands, permissions, placeholders, config, health, and the public docs/debug URL. `/nick debug presets` adds a Nick-specific preset list, and `/nick debug presets validate` checks loaded preset definitions for common mistakes.

## Permissions

```text
onembcmi.nick.use
onembcmi.nick.set
onembcmi.nick.bio
onembcmi.nick.rate
onembcmi.nick.share
onembcmi.nick.realname
onembcmi.nick.admin
onembcmi.nick.admin.inspect
onembcmi.nick.admin.reset
onembcmi.nick.admin.review
onembcmi.nick.admin.reload
onembcmi.nick.bypass.cooldown
onembcmi.nick.bypass.related
```

Player nickname permissions default to false except `onembcmi.nick.realname`, which defaults to true. Admin and bypass permissions default to operator/admin scope.

Preset-specific permissions are configurable in the preset files. Defaults include examples such as:

```text
onembcmi.nick.style.unique.winter_star
onembcmi.nick.style.unique.halloween_glow
onembcmi.nick.style.group.player
onembcmi.nick.style.group.member
onembcmi.nick.style.group.builder
onembcmi.nick.style.milestone.playtime_50
onembcmi.nick.style.milestone.jobs_100
onembcmi.nick.style.milestone.mcmmo_500
onembcmi.nick.style.milestone.pyrofishing_100
onembcmi.nick.style.milestone.pyromining_100
onembcmi.nick.style.milestone.pyrofarming_100
onembcmi.nick.style.milestone.all_skills_1000
```

## Config Files

Nick writes commented files under:

```text
plugins/1MB-CMIAPI/Nick/config.yml
plugins/1MB-CMIAPI/Nick/rules.yml
plugins/1MB-CMIAPI/Nick/presets/global.yml
plugins/1MB-CMIAPI/Nick/presets/gradients.yml
plugins/1MB-CMIAPI/Nick/presets/unique.yml
plugins/1MB-CMIAPI/Nick/presets/groups.yml
plugins/1MB-CMIAPI/Nick/presets/economy.yml
plugins/1MB-CMIAPI/Nick/presets/milestones.yml
plugins/1MB-CMIAPI/Nick/presets/moods.yml
plugins/1MB-CMIAPI/Nick/stats.yml
plugins/1MB-CMIAPI/Nick/logs/nick.log
```

`config.yml` controls cooldowns, GUI filler material, CMI command templates, sharing, celebration effects, economy behavior, top-list reward hardening, Style Lab behavior, pending review queue behavior, temporary lock limits, Discord staff alerts, unlock receipts, locked-style showcase visibility, Nick Museum tracking, milestone progress display, and command allow-lists.

Each cleanup/randomizer tool can be individually enabled or disabled in `config.yml`. Disabled tools are hidden from the Tools GUI, removed from tab completion, and blocked if someone tries to run them manually:

```yaml
tools:
  stripnumbers:
    enabled: true
  lowercase:
    enabled: true
  uppercase:
    enabled: false
  capitalize:
    enabled: true
  initials:
    enabled: true
  random:
    enabled: true
```

`rules.yml` controls nickname relatedness, duplicate-name checks, blocked staff/server terms, filter patterns, and staff alerts.

The `presets/` files define solid colors, gradients, seasonal styles, group styles, economy unlocks, milestones, and mood colors. Each preset can be enabled, disabled, permission-locked, date-locked, month-locked, bold/italic decorated, or given a money/EXP unlock cost.

Milestone presets are permission-based by design. Jobs, mcMMO, PyroFishing, PyroMining, PyroFarming, BirthdayLanterns, Upgrade, or any other reward system can grant the configured style permission when the player reaches the milestone. Nick can also display configured PlaceholderAPI progress rows with `/nick milestones`, the player head in the GUI, and milestone preset lore; this display is read-only and does not grant permissions by itself.

Important legacy paths:

```yaml
legacy:
  enabled: true
  capture-on-join: true
  restore-uses: 1
  reset-if-disabled: true
```

`legacy.enabled: false` stops legacy restores. When `legacy.reset-if-disabled` is also true, Nick checks joining players and players opening `/nick`; if their current CMI nickname still matches the stored legacy nickname, Nick clears the CMI nickname, sets their legacy restore count to `0`, and sends the configured `commands.legacy-disabled-mail` CMI mail. New Nick-generated names are not touched.

## Data

Per-player Nick data is stored in the shared 1MB-CMIAPI playerdata file under the `nick` section. It tracks:

- current plain nickname, style id, and generated CMI formatted nickname
- captured legacy CMI nickname, restore count, capture time, and whether the current nickname is legacy
- last name/style/tool/share/rating timestamps
- unlocked paid styles
- timestamped rating history for top reward checks
- monthly top reward claims
- temporary admin lock state
- used styles for Nick Museum collection display
- opt-in global announcement state
- sanitized bio
- rolling nickname history
- permanent favorite slots
- anonymous rating map keyed by voter UUID

The separate `stats.yml` stores style use counts for trending/debug output and the pending admin review queue. `logs/nick.log` records successful changes, failed changes, filter catches, staff-impersonation attempts, ratings, clears, admin resets, admin locks, unlock receipts, review resolutions, and top reward claims. `/nick admin review logs` reads this log for paged log review.

## Safety Notes

Nick never uses RCON. It only dispatches reviewed direct console commands and blocks command templates containing command chains, pipes, semicolons, tellraw, execute/function, op/deop, reload/stop, or unsafe angle brackets. Trusted CMI hex tokens generated by Nick are allowed through so gradient nicknames still apply.

Player input is sanitized before storage and before display. Bio text preserves `<3`, but removes formatting and unsafe command-looking text.

Duplicate-name checks compare against known Bukkit offline player names so players cannot impersonate another player with a close real-name nickname. The check is intentionally conservative and can be reviewed with `/nick admin inspect <player>`.

## CMI, CMILib, And Hooks

CMI and CMILib are required. Nick delegates final nickname changes through configurable CMI console commands:

```text
cmi nick {nick} {player} -s
cmi nick off {player} -s
cmi broadcast {player} picked a new nickname: {preview}
cmi mail send {player} Your old legacy nickname is no longer supported by the current /nick system. We cleared it so everyone uses the safer Nick picker. Use /nick to choose a new nickname.
cmi realname {player}
```

Vault is optional for money-cost style unlocks. PlaceholderAPI receives the Nick placeholders through the shared library. DiscordSRV can be used by configuring `share.discord-command` or the optional staff alert command. The default Discord alert channel id is `1186634863827234836`, but the command remains configurable because DiscordSRV command syntax may be server-specific. LuckPerms can grant group, event, milestone, and bypass permissions.

The default nickname announcement uses `cmi broadcast`, and Nick adds that prefix to the runtime allow-list when the configured announcement command still uses it. Other custom announcement commands still need a matching entry in `commands.allowed-prefixes`.

## Placeholders

```text
%onembcmi_nick.enabled%
%onembcmi_nick.current.plain%
%onembcmi_nick.current.style%
%onembcmi_nick.current.formatted%
%onembcmi_nick.bio%
%onembcmi_nick.rating.average%
%onembcmi_nick.rating.count%
%onembcmi_nick.cooldown.name.seconds%
%onembcmi_nick.cooldown.style.seconds%
%onembcmi_nick.history.count%
%onembcmi_nick.favorites.count%
%onembcmi_nick.legacy.available%
%onembcmi_nick.legacy.restores_remaining%
%onembcmi_nick.current.legacy%
```

## Future Notes

Offline legacy nickname migration is shelved for a future pass. The current live-safe flow captures legacy nicknames as players join or open `/nick`; a later offline tool could scan CMI storage in bulk if that becomes worth the risk.

Nameplate support is intentionally deferred. When the nickname flow is stable on live, investigate whether per-player nameplates can be changed without breaking glow, scoreboard, tab list, or CMI nameplate behavior. A future prefix/nameplate pass could add safe tools for staff-managed player prefixes and suffixes, plus player-selectable unlocked prefixes from groups, events, milestones, or seasonal rewards.

Chat bubble tinting is also deferred: a future pass can investigate matching chat bubble color to the player's active Nick style.
