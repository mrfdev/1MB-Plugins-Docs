# 1MB AntiFire Placeholders

This plugin does not currently provide a PlaceholderAPI expansion or any custom placeholder tokens.

## Verified Status

- No `%placeholder%` syntax is implemented in this repository.
- No PlaceholderAPI hook or expansion registration exists in the plugin code.
- No MiniMessage placeholder system is exposed for server configuration.

If placeholder support is added in a future release, document the exact placeholder names, output meaning, and any
dependencies before publishing them.
